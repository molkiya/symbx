# SymbX

SymbX — это система для символьной регрессии с использованием обучения с подкреплением (Reinforcement Learning). Проект позволяет находить математические программы, которые преобразуют входные значения в целевые, используя набор дискретных правил (например, сложение и умножение).

## 🎯 Основные возможности

- **Символьная регрессия**: Поиск программ, преобразующих входные значения в целевые
- **Обучение с подкреплением**: Использование RL для обучения политики выбора правил
- **База данных программ**: Хранение и кэширование программ в PostgreSQL
- **REST API**: FastAPI для взаимодействия с системой
- **Хранилище данных**: Интеграция с MinIO (S3-совместимое хранилище)

## 📁 Структура проекта

```
SymbX/
├── main.py                 # Основной модуль обучения RL модели
├── symbx_rules.py          # Определение правил и выполнение программ
├── symbx_db.py             # Работа с БД PostgreSQL и S3
├── app/
│   └── main_api.py         # FastAPI приложение
├── db/
│   └── schema.sql          # Схема базы данных
├── demo/
│   └── demo_register_and_run.py  # Демо скрипт
├── docs/
│   └── run.md              # Инструкции по запуску
├── docker-compose.yml      # Конфигурация инфраструктуры
├── requirements.txt        # Зависимости Python
└── Makefile               # Команды для управления проектом
```

## 🚀 Быстрый старт

### Требования

- Python 3.12+
- Docker и Docker Compose
- PostgreSQL 15 (запускается через Docker)
- MinIO (запускается через Docker)

### Установка

1. **Клонируйте репозиторий** (если еще не сделано)

2. **Создайте виртуальное окружение и установите зависимости:**

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

3. **Запустите инфраструктуру:**

```bash
make infra-up
# или
docker compose up -d
```

Это запустит:
- **PostgreSQL** на порту `5433`
- **MinIO** на портах `9000` (API) и `9001` (веб-консоль)

4. **Создайте bucket в MinIO:**

   - Откройте [http://localhost:9001](http://localhost:9001)
   - Войдите с учетными данными:
     - **Username:** `symbx`
     - **Password:** `replicax12345`
   - Создайте bucket с именем `symbx`

5. **Инициализируйте правила в базе данных:**

```bash
make seed
# или
python3 -c "from symbx_db import SymbXDB; SymbXDB().bootstrap_rules(); print('Seeded rules')"
```

## 📖 Использование

### Обучение модели

Запустите обучение RL модели:

```bash
python main.py
```

Модель будет обучаться на синтетических задачах, используя комбинации правил (сложение и умножение).

### Запуск API сервера

```bash
make api
# или
uvicorn app.main_api:app --reload --host 0.0.0.0 --port 8000
```

API будет доступно по адресу [http://localhost:8000](http://localhost:8000)

### Демонстрация работы

```bash
make curl-demo
```

Или запустите Python скрипт:

```bash
python demo/demo_register_and_run.py
```

## 🔧 API Endpoints

### Health Check
```bash
GET /health
```

### Bootstrap Rules
```bash
POST /rules/bootstrap
```

### Upsert Program
```bash
POST /programs
Content-Type: application/json

{
  "prog_names": ["*3.0", "+2.0", "*2.0"],
  "complexity": 3
}
```

### Execute Program
```bash
POST /execute
Content-Type: application/json

{
  "prog_names": ["*3.0", "+2.0", "*2.0"],
  "a_value": 1.5,
  "computed_by": "cpu"
}
```

### Get Program
```bash
GET /program/{prog_hash}
```

### Log Episode
```bash
POST /episodes
Content-Type: application/json

{
  "experiment_id": 1,
  "prog_names": ["*3.0", "+2.0"],
  "x_pred": 12.0,
  "mse": 0.0,
  "reward": 1.0,
  "steps_count": 2,
  "complexity": 2
}
```

## 🛠️ Makefile команды

- `make infra-up` - Запустить инфраструктуру (PostgreSQL, MinIO)
- `make infra-down` - Остановить инфраструктуру и удалить volumes
- `make api` - Запустить API сервер
- `make psql` - Подключиться к PostgreSQL
- `make seed` - Инициализировать правила в БД
- `make curl-demo` - Запустить демонстрационные запросы
- `make pdf` - Скомпилировать LaTeX документ в PDF

## 🧪 Правила (Rules)

Система использует дискретные правила для построения программ:

- **Сложение**: `+1.0`, `+2.0`, `+3.0`
- **Умножение**: `*2.0`, `*3.0`

Правила можно расширять в файле `symbx_rules.py`.

## 🗄️ База данных

База данных хранит:

- **Правила** (`rule`, `rule_version`) - определения правил и их версии
- **Программы** (`program`, `program_step`) - сохраненные программы
- **Задачи** (`task`, `dataset`) - входные данные для обучения
- **Эпизоды** (`episode`) - результаты выполнения программ
- **Кэш выполнения** (`exec_cache`) - кэшированные результаты

Схема БД находится в `db/schema.sql`.

## 🔍 Архитектура

1. **Обучение** (`main.py`):
   - Генерация синтетических задач
   - Обучение политики с помощью RL (REINFORCE)
   - Имитационное обучение для предварительной подготовки

2. **Выполнение** (`symbx_rules.py`):
   - Определение правил
   - Выполнение программ
   - Каноническая форма и хеширование

3. **Хранение** (`symbx_db.py`):
   - Работа с PostgreSQL
   - Интеграция с MinIO (S3)
   - Кэширование результатов выполнения

4. **API** (`app/main_api.py`):
   - REST API для взаимодействия с системой
   - Управление программами и эпизодами

## 📝 Конфигурация

Настройки можно изменить через переменные окружения или в коде:

- `PG_DSN` - строка подключения к PostgreSQL
- `S3_ENDPOINT` - адрес MinIO
- `S3_ACCESS_KEY` - ключ доступа S3
- `S3_SECRET_KEY` - секретный ключ S3
- `S3_BUCKET` - имя bucket в S3

## 📄 Лицензия

См. файл [LICENSE](LICENSE)

## 🤝 Вклад

Проект находится в активной разработке. Приветствуются предложения и улучшения!

## 📚 Дополнительная документация

- **Математика и алгоритмы** — математическая формулировка задачи, алгоритмы обучения и псевдокод (в разработке)
- **LaTeX документация** — профессиональная PDF версия с формулами (в разработке)

> **Примечание**: Подробная математическая документация находится в разработке. Основная информация о проекте представлена в этом README.


